# Application
VITE_APP_TITLE=React Playwright Framework
VITE_API_URL=http://localhost:3000/api

# Testing
TEST_BASE_URL=http://localhost:5173

# Test Credentials (used by Playwright tests)
# These match the mock users in src/services/authService.ts
TEST_USER_EMAIL=demo@demo.com
TEST_USER_PASSWORD=password123
TEST_ADMIN_EMAIL=admin@demo.com
TEST_ADMIN_PASSWORD=admin123

# Encryption key for secure credential storage (optional, for CI/CD)
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# ENCRYPTION_KEY=your-64-character-hex-key-here
